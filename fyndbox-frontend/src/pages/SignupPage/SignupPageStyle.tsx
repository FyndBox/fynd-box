import { Container} from "@mui/material";
import { styled } from "@mui/material/styles";

export const StyledSignup = styled(Container)({
    // display:"flex",
    // flex-direction: column,
    // align-items: center,
    backgroundColor:'primary.main',
  });